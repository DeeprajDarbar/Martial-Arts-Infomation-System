@model IEnumerable<MartialArtsWebApp.Models.TestFee>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TestFees)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Rank.Rank_Name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TestFees)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Rank.Rank_Name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.TestID }) |
            @Html.ActionLink("Details", "Details", new { id=item.TestID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.TestID })
        </td>
    </tr>
}

</table>
